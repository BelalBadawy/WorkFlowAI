# 03 — Phase Frontend API Client, React Query Hooks & Management UI

**What to build:**
Create frontend API service `phases-api.ts`, custom React Query hook `usePhases.ts`, and full `PhasesManagement.tsx` management page.

**Blocked by:** 02 — Phase CQRS Application Slice & Minimal API Endpoints

**Status:** completed

- [x] Create `phases-api.ts` with typed endpoint functions.
- [x] Create `usePhases.ts` managing React Query cache keys, mutations, optimistic updates, and toast notifications.
- [x] Create `PhasesManagement.tsx` page with search, filter controls, status toggle switch, pagination, Create/Edit dialog modal, and soft delete/restore dialog.
- [x] Register `/admin/phases` route and add sidebar navigation link.
